import React from 'react';
import { DescriptionWrapper } from './styleCuriosidades';

const Description = ({ text }) => {
    return (
        <DescriptionWrapper>{text}</DescriptionWrapper>
    );
}

export default Description;
